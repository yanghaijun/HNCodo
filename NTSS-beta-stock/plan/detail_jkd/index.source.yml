getData_control23_LTheOI: >
  if (!elem) {
    return;
  }

  var data = {
    d1: [],
    d2: [],
    d3: []
  };

  var elem1 = $(elem).find('.myAccount_con')[0];

  $(elem1).find("tr").each(function () {
    var arr = [];
    $(this).find("th").each(function () {
      if ($(this).find('span').length == 0) {
        if ($(this).next().find('select').length > 0) {
          var arrs = [];
          $(this).next().find('select').find('option').each(function () {
            arrs.push($(this).text());
          });
          arr.push({
            title: $(this).text().trim().replace(/\*/, ''),
            mark: '0',
            content: arrs,
            type: 'select'
          });
        } else {
          arr.push({
            title: $(this).text().trim().replace(/\*/, ''),
            mark: '0',
            content: $(this).next().text().trim(),
            type: 'txt'
          });
        }
      } else {
        if ($(this).next().find('select').length > 0) {
          var arrs = [];
          $(this).next().find('select').find('option').each(function () {
            arrs.push($(this).text());
          });
          arr.push({
            title: $(this).text().trim().replace(/\*/, ''),
            mark: '1',
            content: arrs,
            type: 'select'
          });
        } else {
          arr.push({
            title: $(this).text().trim().replace(/\*/, ''),
            mark: '1',
            content: $(this).next().text().trim(),
            type: 'txt'
          });
        }
      }
    });
    data.d1.push(arr);
  });

  var elem2 = $(elem).find('.myAccount_con').eq(1)[0];

  $(elem2).find("tr").each(function (i) {
    if (i > 0) {
      var arr = [];
      $(this).find(".nowarp").each(function (i1) {
        if ($(this).find('span').length == 0) {
          if ($(this).next().find('select').length > 0) {
            var arrs = [];
            $(this).next().find('select').find('option').each(function () {
              arrs.push($(this).text());
            });
            arr.push({
              title: $(this).text().trim().replace(/\*/, ''),
              mark: "0",
              content: arrs,
              type: 'select'
            });
          } else if ($(this).next().find('textarea').length > 0) {
            arr.push({
              title: $(this).text().trim().replace(/\*/, ''),
              mark: "0",
              content: $(this).next().find('textarea').prop('value').trim(),
              type: 'textarea'
            });
          } else if ($(this).next().find('input').length == 1) {
            arr.push({
              title: $(this).text().trim().replace(/\*/, ''),
              mark: "0",
              content: $(this).next().find('input').prop('value').trim(),
              type: 'input'
            });
          } else {
            arr.push({
              title: $(this).text().trim().replace(/\*/, ''),
              mark: "0",
              content: $(this).next().text().trim(),
              type: 'txt'
            });
          }
        } else {
          if ($(this).next().find('select').length > 0) {
            var arrs = [];
            $(this).next().find('select').find('option').each(function () {
              arrs.push($(this).text());
            });
            arr.push({
              title: $(this).text().trim().replace(/\*/, ''),
              mark: "1",
              content: arrs,
              type: 'select'
            });
          } else if ($(this).next().find('textarea').length > 0) {
            arr.push({
              title: $(this).text().trim().replace(/\*/, ''),
              mark: "1",
              content: $(this).next().find('textarea').prop('value').trim(),
              type: 'textarea'
            });
          } else if ($(this).next().find('input').length == 1) {
            arr.push({
              title: $(this).text().trim().replace(/\*/, ''),
              mark: "1",
              content: $(this).next().find('input').prop('value').trim(),
              type: 'input'
            });
          } else {
            arr.push({
              title: $(this).text().trim().replace(/\*/, ''),
              mark: "1",
              content: $(this).next().text().trim(),
              type: 'txt'
            });
          }
        }
      });
      data.d2.push(arr);
    }
  });

  var elem3 = $(elem).children('form').find(".input_table")[0];
  //console.log(elem3)

  $(elem3).find("tr").each(function (i) {
    if (i > 0) {
      var arr = [];
      $(this).find(".nowarp").each(function (i1) {
        if ($(this).find('span').length == 0) {
          if ($(this).next().find('select').length > 0) {
            var arrs = [];
            $(this).next().find('select').find('option').each(function () {
              arrs.push($(this).text());
            });
            arr.push({
              title: $(this).text().trim().replace(/\*/, ''),
              mark: "0",
              content: arrs,
              type: 'select'
            });
          } else if ($(this).next().find('textarea').length > 0) {
            arr.push({
              title: $(this).text().trim().replace(/\*/, ''),
              mark: "0",
              content: $(this).next().find('textarea').prop('value').trim(),
              type: 'textarea'
            });
          } else if ($(this).next().find('input').length == 1 && $(this).next().find('input')[0].type !== 'hidden') {
            arr.push({
              title: $(this).text().trim().replace(/\*/, ''),
              mark: "0",
              content: $(this).next().find('input').prop('value').trim(),
              type: 'input'
            });
          } else {
            arr.push({
              title: $(this).text().trim().replace(/\*/, ''),
              mark: "0",
              content: $(this).next().text().trim(),
              type: 'txt'
            });
          }
        } else {
          if ($(this).next().find('select').length > 0) {
            var arrs = [];
            $(this).next().find('select').find('option').each(function () {
              arrs.push($(this).text());
            });
            arr.push({
              title: $(this).text().trim().replace(/\*/, ''),
              mark: "1",
              content: arrs,
              type: 'select'
            });
          } else if ($(this).next().find('textarea').length > 0) {
            arr.push({
              title: $(this).text().trim().replace(/\*/, ''),
              mark: "1",
              content: $(this).next().find('textarea').prop('value').trim(),
              type: 'textarea'
            });
          } else if ($(this).next().find('input').length == 1) {
            arr.push({
              title: $(this).text().trim().replace(/\*/, ''),
              mark: "1",
              content: $(this).next().find('input').prop('value').trim(),
              type: 'input'
            });
          } else {
            arr.push({
              title: $(this).text().trim().replace(/\*/, ''),
              mark: "1",
              content: $(this).next().text().trim(),
              type: 'txt'
            });
          }
        }
      });
      data.d3.push(arr);
    }
  });

  return data;
doAction_uiControl18_MFLVHe: |
  var event = data.eventType;
  if (event == 'change1') {
    //debugger;
    var no1 = parseInt(data.dataCustom.no1);
    var no2 = parseInt(data.dataCustom.no2);
    var index = data.dataCustom.index;
    var elem1 = $(elem).find('.myAccount_con')[0];
    var elem2 = $(elem).find('.myAccount_con').eq(1)[0];
    $(elem1).find('tr').eq(no1).find('select').eq(no1)[0].selected = true;
  }
  if (event == 'change2') {
    //debugger;
    var no1 = parseInt(data.dataCustom.no1) + 1;
    var no2 = data.dataCustom.no2;
    var index = data.dataCustom.index;
    var elem1 = $(elem).find('.myAccount_con')[0];
    var elem2 = $(elem).find('.myAccount_con').eq(1)[0];
    $(elem2).find('tr').eq(no1).find('select').eq(0).find('option').eq(index)[0].selected = true;
  }
  if (event == 'textarea') {
    var val = data.dataCustom;
    var elem1 = $(elem).find('.myAccount_con')[0];
    var elem2 = $(elem).find('.myAccount_con').eq(1)[0];
    $(elem2).find('textarea').prop('value', val);
  }
  if (event == 'input') {
    //debugger;
    var no1 = parseInt(data.dataCustom.no1) + 1;
    var val = data.dataCustom.value;
    var elem1 = $(elem).find('.myAccount_con')[0];
    var elem2 = $(elem).find('.myAccount_con').eq(1)[0];
    $(elem2).find('tr').eq(no1).find('input').eq(0).prop('value', val);
  }
  if (event == 'input3') {
    //debugger;
    var no1 = parseInt(data.dataCustom.no1) + 1;
    var val = data.dataCustom.value;
    var elem3 = $(elem).children('form').find(".input_table")[0];
    $(elem3).find('tr').eq(no1).find('input').eq(0).prop('value', val);
  }
getTemplate_uiControl18_MFLVHe: |
  module.exports = React.createClass({
    textarea:function(e){
      var handler = this.props.customHandler;
      if(handler){
        handler({
          data:e.target.value,
          eventType:'textarea' 
        })
      }
    },
    input:function(e){
      var handler = this.props.customHandler;
      if(handler){
        handler({
          data:{
            value:e.target.value,
            no1:e.target.getAttribute('data-index1'),
          },
          eventType:'input' 
        })
      }
    },
    input3:function(e){
      var handler = this.props.customHandler;
      if(handler){
        handler({
          data:{
            value:e.target.value,
            no1:e.target.getAttribute('data-index1'),
          },
          eventType:'input3' 
        })
      }
    },
    change1:function(e){
      var handler = this.props.customHandler;
      if(handler){
        handler({
          data:{
            index:e.target.selectedIndex,
            no1:e.target.getAttribute('data-index1'),
            no2:e.target.getAttribute('data-index2'),
          },
          eventType:'change1' 
        })
      }
    },
     change2:function(e){
      var handler = this.props.customHandler;
      if(handler){
        handler({
          data:{
            index:e.target.selectedIndex,
            no1:e.target.getAttribute('data-index1'),
            no2:e.target.getAttribute('data-index2'),
          },
          eventType:'change2' 
        })
      }
    },
    render: function() {
      var data = this.props.customData;
      var _this = this;
    var item = data && data.d1.map(function(d,i){
      return(<li>
        
        {
            d.map(function(d2,i2){
        if(d2.type=='txt'){
           return (<div>
               {d2.mark=='0' ?
                 <div className='title'>{d2.title}</div>
                 :
                 <div className='title'><span style={{color:'red'}}>*</span>{d2.title}</div>
               }
               <div className='content'>{d2.content}</div>
             
             </div>)
         }
        else if(d2.type=='select'){
          var select = d2.content.map(function(d1,i1){
            return (<option>{d1}</option>)
          })
           return (<div>{d2.mark=='0' ?
                 <div className='title'>{d2.title}</div>
                 :
                 <div className='title'><span style={{color:'red'}}>*</span>{d2.title}</div>
               }<div className='content'><select data-index1={i} data-index2={i2} onChange={_this.change1}>{select}</select></div></div>)
        }
      })
          }
        </li>)
      
     })
    
     var item2 = data && data.d2.map(function(d,i){
      return(<li>
        
        {
            d.map(function(d2,i2){
        if(d2.type=='txt'){
           return (<div> {d2.mark=='0' ?
                 <div className='title'>{d2.title}</div>
                 :
                 <div className='title'><span style={{color:'red'}}>*</span>{d2.title}</div>
               }<div className='content'>{d2.content}</div></div>)
         }
       else if(d2.type=='input'){
           return (<div> {d2.mark=='0' ?
                 <div className='title'>{d2.title}</div>
                 :
                 <div className='title'><span style={{color:'red'}}>*</span>{d2.title}</div>
               }
               <div className='content'><AInput onBlur={_this.input} data-index1={i} value={d2.content} /></div></div>)
         }
       else if(d2.type=='textarea'){
           return (<div> {d2.mark=='0' ?
                 <div className='title'>{d2.title}</div>
                 :
                 <div className='title'><span style={{color:'red'}}>*</span>{d2.title}</div>
               }<div className='content'><ATextarea onBlur={_this.textarea} value={d2.content} /></div></div>)
         }
        else if(d2.type=='select'){
          var select = d2.content.map(function(d1,i1){
            return (<option>{d1}</option>)
          })
           return (<div> {d2.mark=='0' ?
                 <div className='title'>{d2.title}</div>
                 :
                 <div className='title'><span style={{color:'red'}}>*</span>{d2.title}</div>
               }<div className='content'><select data-index1={i} data-index2={i2} onChange={_this.change2}>{select}</select></div></div>)
        }
      })
          }
        </li>)
      
     })
    var item3 = data && data.d3.map(function(d,i){
      return(<li>
        
        {
            d.map(function(d2,i2){
        if(d2.type=='txt'){
           return (<div> {d2.mark=='0' ?
                 <div className='title'>{d2.title}</div>
                 :
                 <div className='title'><span style={{color:'red'}}>*</span>{d2.title}</div>
               }<div className='content'>{d2.content}</div></div>)
         }
       else if(d2.type=='input'){
           return (<div> {d2.mark=='0' ?
                 <div className='title'>{d2.title}</div>
                 :
                 <div className='title'><span style={{color:'red'}}>*</span>{d2.title}</div>
               }
               <div className='content'><AInput onBlur={_this.input3} data-index1={i} value={d2.content} /></div></div>)
         }
       else if(d2.type=='textarea'){
           return (<div> {d2.mark=='0' ?
                 <div className='title'>{d2.title}</div>
                 :
                 <div className='title'><span style={{color:'red'}}>*</span>{d2.title}</div>
               }<div className='content'><ATextarea onBlur={_this.textarea} value={d2.content} /></div></div>)
         }
        else if(d2.type=='select'){
          var select = d2.content.map(function(d1,i1){
            return (<option>{d1}</option>)
          })
           return (<div> {d2.mark=='0' ?
                 <div className='title'>{d2.title}</div>
                 :
                 <div className='title'><span style={{color:'red'}}>*</span>{d2.title}</div>
               }<div className='content'><select data-index1={i} data-index2={i2} onChange={_this.change2}>{select}</select></div></div>)
        }
      })
          }
        </li>)
      
     })
    
      return (
        <div className='faqiBox_lfj'>
         <ul className='Basic'><span className='icon-title'>员工借款单</span>{item}</ul>
          <ul className='Basic'><span className='icon-title'>借款信息</span>{item2}</ul>
          <ul className='Basic'><span className='icon-title'>收款信息</span>
            <h4 style={{marginLeft:'10px'}}>员工个人收款信息</h4>
            {item3}</ul>
        </div>
      )
    }
  });
getData_control28_mZJdMl: |
  if (!elem) {
    return;
  }
  var data = [];
  var elem = $(elem).children('tbody').eq(1)[0];
  if ($(elem).children("tr").length > 0) {
    $(elem).children("tr").each(function () {
      var arr = [];

      if ($(this).children("td").eq(2).text().indexOf('txt') !== -1) {
        arr.push({
          type: 'txt',
          content: $(this).children('td').eq(2).text(),
          time: $(this).children('td').eq(3).text(),
          leixing:$(this).children('td').eq(1).text()
        });
      }
      else if ($(this).children("td").eq(2).text().indexOf('png') !== -1) {
        arr.push({
          type: 'png',
          content: $(this).children('td').eq(2).text(),
          time: $(this).children('td').eq(3).text(),
          leixing:$(this).children('td').eq(1).text()
        });
      }
      else if ($(this).children("td").eq(2).text().indexOf('jpg') !== -1) {
        arr.push({
          type: 'png',
          content: $(this).children('td').eq(2).text(),
          time: $(this).children('td').eq(3).text(),
          leixing:$(this).children('td').eq(1).text()
        });
      }
      else if ($(this).children("td").eq(2).text().indexOf('gif') !== -1) {
        arr.push({
          type: 'gif',
          content: $(this).children('td').eq(2).text(),
          time: $(this).children('td').eq(3).text(),
          leixing:$(this).children('td').eq(1).text()
        });
      }
      else {
        arr.push({
          type: 'known',
          content: $(this).children('td').eq(2).text(),
          time: $(this).children('td').eq(3).text(),
          leixing:$(this).children('td').eq(1).text()
        });
      }
      data.push(arr);
    });
  }
  return data;
doAction_uiControl22_svg3yy: |-
  //附件类型:{d[0].leixing}
  if(data.eventType=='click'){
    debugger;
    var idx = data.dataCustom;
    var elem = $(elem).children('tbody').eq(1)[0];
    $(elem).children('tr').eq(idx).children('td').eq(0).find('a').click()
  }
getTemplate_uiControl22_svg3yy: "module.exports = React.createClass({\n  click:function(e){\n    var handler = this.props.customHandler;\n    if(handler){\n      handler({\n        eventType:'click',\n        data:e.target.getAttribute('data-index')\n      })\n    }\n     // handler({\n     //   eventType:'section-trigger',\n     //   data:'control36_9TGmS7'\n     // })\n  },\n  render: function() {\n    var data = this.props.customData;\n    var _this = this;\n    var item = data && data.map(function(d,i){\n\t\t\t\treturn (\n         <div data-index={i} class=\"file\">\n            <i class=\"icon\"></i>\n            <div class=\"center\">\n              <div>{d[0].content}</div>\n              <div>{d[0].time}</div>\n            </div>\n            <div data-index={i} onClick={_this.click} class=\"right\">\n              <span data-index={i}>X</span>\n            </div>\n          </div>\n        )\n    })\n    if(data && data.length){\n       return (\n        <div className='ysp-file2'>\n          {item}\n        </div>\n      )\n    }\n   \telse{\n       return (\n        <div>\n        </div>\n      )\n    }\n  }\n});\n\n"
getTemplate_uiControl26_rxiDqc: "module.exports = React.createClass({\n  click:function(e){\n    var handler = this.props.customHandler;\n    if(handler){\n      handler({\n        eventType:'click'\n      })\n    }\n  },\n  render: function() {\n    var _this = this;\n    var data = this.props.customData;\n    return (\n      <div>\n        <div onClick={_this.click} className='fjsc_lfj'>\n          上传附件\n        </div>\n        <div className='fjscwj_lfj'>\n       \t {data && data.size && data.size < 800*1024 ? data.text : ''}\n     \t\t </div>\n      </div>\n    )\n  }\n});"
getData_control32_0wrqn3: |
  if(!elem){
    return ;
  }
  if(elem.files[0] && elem.files[0].size){
    return {
      text: elem.value,
      size:elem.files[0].size
    }
  }
  else{
    return {
      text: elem.value,
    }
  }
doAction_uiControl26_rxiDqc: |-
  if(data.eventType == 'click'){
    elem.click();
  }
getTemplate_uiControl28_3TVHCP: |-
  module.exports = React.createClass({
    click:function(e){
      var handler = this.props.customHandler;
      if(handler){
        handler({
          eventType:'click'
        })
      }
    },
    render: function() {
      var _this = this;
      var data = this.props.customData;
      if(data){
         return (
          <div className='weng_sucessMesbx2'>
            <h5>提示</h5>
            <p>{data[0].content}</p>
            <p onClick={_this.click}><button>{data[0].btn}</button></p>
          </div>
        )
      }
      else{
        return(<div></div>)
      }
     
    }
  });
getData_control34_rDTduT: >-
  if (!elem) {
    return;
  }

  var data = [];

  if (elem.querySelector('iframe') &&
  elem.querySelector('iframe').contentDocument.querySelector('table')) {
    var elem = elem.querySelector('iframe').contentDocument.querySelector('table');
    data.push({
      content: $(elem).find('td').eq(0).text().trim(),
      btn: $(elem).find('td').eq(1).text().trim()
    });
  }

  return data;
doAction_uiControl28_3TVHCP: |-
  if (data.eventType == 'click') {
    debugger;

    //if (elem.querySelector('iframe') && elem.querySelector('iframe').contentDocument.querySelector('table')) {
      //var elem = elem.querySelector('iframe').contentDocument.querySelector('table');
      $(elem).find('a').eq(0).click();
      setTimeout(function(){ysp.appMain.hideLoading()}, 1000);
    //}
  }
getTemplate_uiControl17_SHx7VQ: "module.exports = React.createClass({\n  click1:function(){\n    var handler = this.props.customHandler;\n    if(handler){\n      handler({\n        eventType:'click1'\n      })\n    }\n     handler({\n       eventType:'section-trigger',\n       data:'control36_9TGmS7'\n     })\n  },\n   click2:function(){\n    var handler = this.props.customHandler;\n    if(handler){\n      handler({\n        eventType:'click2'\n      })\n    }\n      handler({\n       eventType:'section-trigger',\n       data:'control34_ZwRZrX'\n     })\n  },\n  render: function() {\n    var _this= this;\n    return (\n      <div className='bar2_lfj'>\n        <div onClick={_this.click1}>\n        \t<span>\n          \t保存\n          </span>\n        </div>\n         <div onClick={_this.click2}>\n        \t<span>\n          \t提交\n          </span>\n        </div>\n      </div>\n    )\n  }\n});\n"
getData_control20_PvWO7F: |-

  if (!elem) {
    return;
  }
  var data = [];
  return data;
doAction_uiControl17_SHx7VQ: |
  var event = data.eventType;
  if (event == 'click1') {
    // debugger;
    $(elem).find('input').eq(0).click();
  }
  if (event == 'click2') {
    $(elem).find('input').eq(1).click();
  }
getTemplate_uiControl23_Vewx3L: "import {Component} from 'react';\nexport default class extends Component{\n  render(){\n    return (\n    \t<div className = 'TodoList-head'>\n      \t<span onClick={()=>{\n            var handler = this.props.customHandler;\n            if(handler){\n              handler({\n                eventType:'click'\n              })\n            }\n          }}>返回</span>\n        <span>{this.props.customData}</span>\n      </div>\n    )\n  }\n}"
getData_control29_kQUHiF: |
  if (!elem) {
    return false;
  }
  //console.log(elem)
  return elem.ownerDocument.querySelector('span.path').textContent;
doAction_uiControl23_Vewx3L: |-
  if (data.eventType == 'click') {
    elem.ownerDocument.querySelector('#_easyui_tree_3').click();
    ysp.appMain.showLoading();
    setTimeout(function () {
      ysp.appMain.hideLoading();
    }, 2000);
  }
getTemplate_uiControl32_ETX8Ck: |-
  module.exports = React.createClass({
    click:function(e){
      var handler = this.props.customHandler;
      if(handler){
        handler({
          eventType:'click'
        })
      }
       handler({
         eventType:'section-trigger',
         data:'control36_9TGmS7'
       })
    },
    render: function() {
      var _this = this;
      return (
        <div onClick={_this.click} style={{margin:'10px'}}>
         <button style={{heigth:'35px',lineHeight:'35px'}} className='bcfj_lfj'>保存附件</button>
        </div>
      )
    }
  });
getData_control26_lfpuE6: |-
  if(!elem){
    return ;
  }
  return [];
doAction_uiControl32_ETX8Ck: "if(data.eventType=='click'){\n  elem.click();\n  //alert(elem.textContent)\n  ysp.appMain.showLoading();\n  setTimeout(function(){\n    \tysp.appMain.hideLoading()\n  },1000)\n}"
getTemplate_uiControl33_hZRi9T: |-
  module.exports = React.createClass({
    // getInitialState: function () {
    //   return {
    //     size: this.props.customData
    //   };
    // },
    componentDidUpdate(){
      debugger;
      var size = this.props.customData;
        if(size>800*1024){
           alert('文件超过限制大小(800KB)'); top.document.querySelector('.bcfj_lfj').disabled=true;
          //console.log(document)
        }
      else{
       top.document.querySelector('.bcfj_lfj').disabled=false;
      }
    },
    render: function() {
      return (
        <div style={{display:"none"}}>
          自定义组件用来适配基本组件无法适配的页面元素，您可以通过右键打开该自定义组件编辑器进行编辑
        </div>
      )
    }
  });
getData_control38_WrOWdG: "if(!elem.files){\n  return ;\n}\nif(elem.files && elem.files[0]){\n\treturn elem.files[0].size\n}\n"
doAction_uiControl33_hZRi9T: ' '
